{
  "name": "demo-selenium-yatt-pipe",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "selenium": "mocha --no-timeouts --recursive --reporter mocha-junit-reporter --reporter-options mochaFile=results/selenium-results.xml"
  },
  "license": "ISC",
  "dependencies": {
    "chai": "^4.3.7",
    "chromedriver": "^107.0.3",
    "mocha": "^10.1.0",
    "mocha-junit-reporter": "^2.0.0",
    "selenium-webdriver": "^4.6.1"
  }
}